# Hardhat setup

cd contracts/src/main
npm init -y
npx truffle init
npm install --save-dev hardhat
npx hardhat

# Compile contracts
npx hardhat compile

# To do
move project to root (out of src/main)

